import React from 'react';

const ReviewDetails = (props) => {
  console.log(props);
    return (
        <div>
            {/* <img src={img} alt="" /> */}
        </div>
    );
};

export default ReviewDetails;